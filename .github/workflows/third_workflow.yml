name: Third Workflow
on: workflow_dispatch

jobs:
  third-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greetings
        run: echo "hello world"
      - name: print goodbye
        run: echo "goodbye"
        
